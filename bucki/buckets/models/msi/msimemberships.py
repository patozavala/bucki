from bucki.buckets.models.memberships import Membership


class MSIMembership(Membership):
    """
    Membership for MSIBucket, inherit from Invitation.
    """
    # bucket = models.ForeignKey('', on_delete=CASCADE)
